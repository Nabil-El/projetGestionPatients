package com.example.demo.PatientDocument;
/*
 * import java.util.List; import java.util.Optional;
 * 
 * import org.springframework.beans.factory.annotation.Autowired; import
 * com.example.demo.document.Document; import
 * com.example.demo.document.DocumentRepository; import
 * com.example.demo.patient.Patient; import
 * com.example.demo.patient.PatientRepository;
 * 
 * public class PatientDocumentServices {
 * 
 * @Autowired private DocumentRepository documentServices; PatientRepository
 * patientServices; List<Document> document = null; Document doc; Patient p;
 * // @Override
 * 
 * public List<Document> findDocumentByIdPatient(Long idpatient) {
 * 
 * List<Document> li=documentServices.findAll();
 * p=patientServices.getOne(idpatient); for (Document d:li) {
 * if(p.getNomPatient()==d.getNomPatient()) { document.add(d); } } return
 * document;
 * 
 * }
 * 
 * 
 * //return documentServices.findById(idpatient); }
 * 
 * 
 * }
 */
